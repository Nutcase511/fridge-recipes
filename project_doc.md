# 智能菜谱生成器 — 项目文档

## 一、项目概述

### 1.1 项目背景
本项目是一个多端通用的小程序，旨在帮助用户解决"冰箱里有菜但不知道做什么"的日常难题。用户只需记录冰箱中的食材和家里的调料，系统即可智能匹配并推荐合适的菜谱。

### 1.2 核心功能
| 功能 | 描述 |
|------|------|
| 🥬 食材管理 | 记录冰箱中的食材名称、分类、数量、保质期 |
| 🧂 调料管理 | 记录家中的调料名称、分类、数量 |
| 🍳 智能生成菜谱 | 根据现有食材和调料，自动匹配推荐菜谱 |
| 🔢 指定菜数 | 用户可选择生成 1-5 道菜 |
| 📄 菜谱详情 | 查看菜谱的完整用料清单和烹饪步骤 |

---

## 二、技术架构

### 2.1 技术选型
```
┌──────────────────────────────────────────────────┐
│                  多端应用                         │
│  ┌──────────┬──────────┬──────────┬──────────┐   │
│  │   H5     │  微信    │  支付宝  │  App     │   │
│  │  网页    │  小程序  │  小程序  │          │   │
│  └──────────┴──────────┴──────────┴──────────┘   │
│  ┌──────────────────────────────────────────────┐ │
│  │           Uni-app (Vue 3 + TS)              │ │
│  └──────────────────────────────────────────────┘ │
│  ┌─────────────┬──────────────┬─────────────────┐ │
│  │   Pinia     │  Vite        │  uni.storage    │ │
│  │  状态管理   │  构建工具    │  本地持久化     │ │
│  └─────────────┴──────────────┴─────────────────┘ │
│  ┌──────────────────────────────────────────────┐ │
│  │        菜谱匹配引擎 (rule-based)             │ │
│  └──────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

### 2.2 项目结构
```
c:/test/
├── .trae/
│   └── project_rules.md        # 项目规则配置
├── src/
│   ├── pages/                  # 页面
│   │   ├── index/              # 首页 - 冰箱概览
│   │   │   └── index.vue
│   │   ├── ingredients/        # 食材管理页
│   │   │   └── index.vue
│   │   ├── seasonings/         # 调料管理页
│   │   │   └── index.vue
│   │   ├── generate/           # 生成菜谱页
│   │   │   └── index.vue
│   │   └── recipe-detail/      # 菜谱详情页
│   │       └── index.vue
│   ├── components/             # 公共组件（预留）
│   ├── stores/                 # Pinia 状态管理
│   │   ├── ingredient.ts       # 食材状态
│   │   ├── seasoning.ts        # 调料状态
│   │   └── recipe.ts           # 菜谱状态
│   ├── utils/                  # 工具函数
│   │   ├── storage.ts          # 本地存储封装
│   │   └── recipeEngine.ts     # 菜谱匹配引擎
│   ├── data/                   # 数据层
│   │   ├── types.ts            # TypeScript 类型定义
│   │   └── recipes.ts          # 内置菜谱库（35道）
│   ├── static/                 # 静态资源
│   ├── App.vue                 # 根组件
│   ├── main.ts                 # 入口文件
│   ├── pages.json              # 页面路由配置
│   ├── manifest.json           # 应用配置
│   └── uni.scss                # 全局样式
├── index.html                  # H5 入口
├── package.json                # 依赖配置
├── tsconfig.json               # TypeScript 配置
└── vite.config.ts              # Vite 配置
```

---

## 三、数据结构设计

### 3.1 食材 (Ingredient)
```typescript
interface Ingredient {
  id: string           // 唯一标识
  name: string         // 食材名称
  category: Category   // 分类：蔬菜/肉类/海鲜/豆制品/蛋奶/水果/主食/其他
  quantity: number     // 数量
  unit: string         // 单位：个/克/根/袋
  expiryDate?: string  // 保质期（可选）
  createTime: string   // 创建时间
}
```

### 3.2 调料 (Seasoning)
```typescript
interface Seasoning {
  id: string           // 唯一标识
  name: string         // 调料名称
  category: Category   // 分类：基础调料/酱料/香料/油类/料酒醋类/其他
  quantity: number     // 数量
  unit: string         // 单位：瓶/袋/克
  createTime: string   // 创建时间
}
```

### 3.3 菜谱 (Recipe)
```typescript
interface Recipe {
  id: string                       // 唯一标识
  name: string                     // 菜名
  description: string              // 描述
  difficulty: '简单'|'中等'|'困难' // 难度
  cookingTime: number              // 烹饪时间（分钟）
  cuisine: string                  // 菜系
  ingredients: RecipeIngredient[]  // 用料清单
  steps: string[]                  // 烹饪步骤
  tips?: string                    // 小贴士
}
```

---

## 四、菜谱匹配算法

### 4.1 匹配流程
```
用户食材列表 ──┐
               ├──→ 遍历所有菜谱 ──→ 计算匹配得分 ──→ 过滤(≥50%) ──→ 排序 ──→ 取前N道
用户调料列表 ──┘
```

### 4.2 评分规则
- **必需食材**：有则 +2 分，无则不加分
- **可选食材**：有则 +1 分，无则不加分
- **匹配率** = (实际得分 / 总分) × 100%
- **门槛**：匹配率 ≥ 50% 的菜谱才会被推荐
- **排序**：按匹配率从高到低排列

### 4.3 内置菜谱库
目前已内置 **35 道** 家常菜谱，涵盖：
- 川菜系列：麻婆豆腐、鱼香肉丝、宫保鸡丁、干煸四季豆、回锅肉
- 经典家常：番茄炒蛋、青椒肉丝、红烧排骨、可乐鸡翅等
- 素菜系列：醋溜白菜、地三鲜、蚝油生菜等
- 汤品类：紫菜蛋花汤、玉米排骨汤、酸辣汤等

---

## 五、页面功能说明

### 5.1 首页（冰箱概览）
- 显示冰箱食材数量概览
- 按分类展示食材分布
- 临期/过期食材提醒
- 随机推荐 4 道菜谱
- 快速跳转到各功能页面

### 5.2 食材管理
- 食材列表展示（名称、分类、数量、保质期）
- 搜索过滤
- 添加/编辑/删除食材
- 选择分类、填写数量单位、可选保质期
- 过期/临期标签高亮

### 5.3 调料管理
- 调料列表展示（名称、分类、数量）
- 搜索过滤
- 添加/编辑/删除调料
- 选择分类、填写数量单位

### 5.4 生成菜谱
- 展示当前食材和调料总数
- 选择生成菜谱数量（1-5道）
- 一键生成
- 结果显示匹配度、缺少食材/调料提示
- 点击进入详情

### 5.5 菜谱详情
- 菜名、描述、难度、用时
- 完整用料清单（区分必需/可选，食材/调料）
- 分步骤烹饪指南
- 小贴士

---

## 六、运行指南

### 6.1 开发环境要求
- Node.js ≥ 18
- npm ≥ 8

### 6.2 安装与运行
```bash
# 进入项目目录
cd c:/test

# 安装依赖
npm install

# 启动 H5 开发模式（浏览器访问）
npm run dev:h5

# 构建微信小程序
npm run build:mp-weixin
# 然后在微信开发者工具中导入 dist/dev/mp-weixin
```

### 6.3 多端支持
| 平台 | 命令 | 说明 |
|------|------|------|
| H5 网页 | `npm run dev:h5` | 浏览器直接访问 |
| 微信小程序 | `npm run dev:mp-weixin` | 需微信开发者工具 |
| 支付宝小程序 | 配置后使用 `dev:mp-alipay` | 需修改 manifest.json |
| App | 使用 HBuilderX 打包 | 需 DCloud 账号 |

---

## 七、后续规划

### 7.1 可扩展方向
- [ ] 云端同步：支持多设备数据同步
- [ ] 菜谱社区：用户可分享自定义菜谱
- [ ] 拍照识别：拍照识别食材自动录入
- [ ] 营养分析：计算每道菜的营养成分
- [ ] 购物清单：根据菜谱自动生成购物清单
- [ ] 智能推荐：基于历史记录推荐个性化菜谱
- [ ] 多人份量：根据用餐人数调整用量

### 7.2 数据备份
所有数据存储在本地 `uni.storage` 中，以 `recipe_` 为前缀：
- `recipe_ingredients` — 食材列表
- `recipe_seasonings` — 调料列表

---

## 八、版本记录

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0.0 | 2026-05-12 | 初始版本：食材/调料管理、菜谱匹配生成、35道内置菜谱 |

---

*本文档由 AI 自动生成，如有变更请及时更新。*